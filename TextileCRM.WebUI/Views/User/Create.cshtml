@model TextileCRM.Domain.Entities.User

@{
    ViewData["Title"] = "Yeni Kullanıcı Ekle";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">
            <!-- Başlık Bölümü -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="mb-1">
                        <i class="bi bi-person-plus-fill text-danger"></i> Yeni Kullanıcı Ekle
                    </h1>
                    <p class="text-muted mb-0">
                        <i class="bi bi-info-circle"></i> Sistem kullanıcısı oluşturun
                    </p>
                </div>
                <a asp-action="Index" class="btn btn-outline-secondary">
                    <i class="bi bi-arrow-left"></i> Geri Dön
                </a>
            </div>

            <div class="card shadow-sm">
                <div class="card-body p-4">
                    <form asp-action="Create">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
                        
                        <!-- Hesap Bilgileri -->
                        <div class="mb-4">
                            <h5 class="border-bottom pb-2 mb-3">
                                <i class="bi bi-shield-fill text-danger"></i> Hesap Bilgileri
                            </h5>
                            
                            <div class="mb-3">
                                <label asp-for="Username" class="form-label fw-semibold">
                                    <i class="bi bi-person-circle"></i> Kullanıcı Adı <span class="text-danger">*</span>
                                </label>
                                <input asp-for="Username" class="form-control" placeholder="Kullanıcı adı" required />
                                <span asp-validation-for="Username" class="text-danger"></span>
                            </div>
                            
                            <div class="mb-3">
                                <label asp-for="PasswordHash" class="form-label fw-semibold">
                                    <i class="bi bi-key"></i> Şifre <span class="text-danger">*</span>
                                </label>
                                <input asp-for="PasswordHash" type="password" class="form-control" placeholder="••••••••" required />
                                <small class="form-text text-muted">Güçlü bir şifre kullanın</small>
                                <span asp-validation-for="PasswordHash" class="text-danger"></span>
                            </div>
                            
                            <div class="mb-3">
                                <label asp-for="Role" class="form-label fw-semibold">
                                    <i class="bi bi-star"></i> Rol <span class="text-danger">*</span>
                                </label>
                                <select asp-for="Role" class="form-control" required>
                                    <option value="Admin">🛡️ Admin - Tam Yetki</option>
                                    <option value="Manager">⭐ Manager - Yönetici</option>
                                    <option value="User" selected>👤 User - Kullanıcı</option>
                                </select>
                                <span asp-validation-for="Role" class="text-danger"></span>
                            </div>
                            
                            <div class="mb-3">
                                <div class="form-check form-switch">
                                    <input asp-for="IsActive" class="form-check-input" type="checkbox" role="switch" checked />
                                    <label asp-for="IsActive" class="form-check-label fw-semibold">
                                        <i class="bi bi-check-circle"></i> Hesap Aktif
                                    </label>
                                </div>
                                <small class="form-text text-muted">Aktif olmayan kullanıcılar giriş yapamaz</small>
                            </div>
                        </div>

                        <!-- Form Butonları -->
                        <div class="d-flex gap-2 justify-content-end pt-3 border-top">
                            <a asp-action="Index" class="btn btn-secondary">
                                <i class="bi bi-x-circle"></i> İptal
                            </a>
                            <button type="submit" class="btn btn-danger btn-lg">
                                <i class="bi bi-check-circle-fill"></i> Kullanıcı Oluştur
                            </button>
                        </div>
                    </form>
                </div>
            </div>

            <!-- Bilgilendirme Kartı -->
            <div class="alert alert-warning mt-3 d-flex align-items-start">
                <i class="bi bi-shield-exclamation fs-4 me-3"></i>
                <div>
                    <strong>Dikkat:</strong> Admin yetkisi verilen kullanıcılar sistemin tüm alanlarına erişebilir.
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
